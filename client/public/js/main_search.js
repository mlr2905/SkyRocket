import { SearchController } from './classes/SearchController.js'; // Ensure correct path

document.addEventListener('DOMContentLoaded', () => {
    // Instantiate the controller to start the application
    new SearchController();
});